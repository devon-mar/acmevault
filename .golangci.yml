---
version: "2"
linters:
  enable:
    - bodyclose
    - copyloopvar
    - forbidigo
    - misspell
    - sloglint
  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close

formatters:
  enable:
    - gofumpt
